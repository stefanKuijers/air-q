import { NextPage } from 'next';

import Layout from '../components/Layout';

const Home: NextPage = () => {
    return (
        <Layout>
            <h1>Home</h1>
            <p>userAgent?</p>
        </Layout>
    );
};

export default Home;
